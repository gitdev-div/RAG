# .flaskenv — Flask development settings.
# Loaded automatically by python-dotenv when you run `flask run`.
# This file is git-ignored (contains local config, not secrets).

FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_RUN_PORT=5000
FLASK_RUN_HOST=127.0.0.1
